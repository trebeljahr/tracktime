{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "tracktime-raycast",
  "title": "tracktime",
  "description": "Start, stop and edit tracktime timers from Raycast, with a live menu bar clock.",
  "icon": "icon.png",
  "author": "trebeljahr",
  "categories": ["Productivity"],
  "license": "AGPL-3.0-or-later",
  "private": true,
  "type": "module",
  "commands": [
    {
      "name": "menu-bar",
      "title": "Timer Menu Bar",
      "subtitle": "tracktime",
      "description": "Show the running timer in the macOS menu bar.",
      "mode": "menu-bar",
      "interval": "30s",
      "preferences": [
        {
          "name": "titleMode",
          "title": "Menu Bar Title",
          "description": "What the menu bar shows while a timer runs.",
          "type": "dropdown",
          "required": false,
          "default": "duration",
          "data": [
            { "title": "Duration only", "value": "duration" },
            { "title": "Description only", "value": "description" },
            { "title": "Duration and description", "value": "both" },
            { "title": "Icon only", "value": "icon" }
          ]
        },
        {
          "name": "tickSeconds",
          "title": "Clock",
          "label": "Count the seconds up while a timer runs",
          "description": "Keeps the command loaded so the menu bar clock moves every second. Off shows minutes, refreshed on the command's interval.",
          "type": "checkbox",
          "required": false,
          "default": true
        },
        {
          "name": "hideWhenIdle",
          "title": "Idle",
          "label": "Hide the menu bar item when no timer runs",
          "description": "Keeps the menu bar clean; the item reappears on the next start.",
          "type": "checkbox",
          "required": false,
          "default": false
        }
      ]
    },
    {
      "name": "timer",
      "title": "Timer",
      "subtitle": "tracktime",
      "description": "Start or stop tracking — the running timer, favorites and recent work.",
      "mode": "view",
      "keywords": ["start", "stop", "toggle", "track", "resume", "pause"]
    },
    {
      "name": "entries",
      "title": "Show All Time",
      "subtitle": "tracktime",
      "description": "Browse, continue, edit and delete recent time entries.",
      "mode": "view",
      "keywords": ["entries", "history", "log", "today", "week"]
    },
    {
      "name": "open-dashboard",
      "title": "Open Dashboard",
      "subtitle": "tracktime",
      "description": "Open the tracktime web app, where reports, invoices and catalog editing live.",
      "mode": "no-view",
      "keywords": ["web", "reports", "invoices", "projects", "clients", "tags"]
    }
  ],
  "preferences": [
    {
      "name": "apiUrl",
      "title": "API URL",
      "description": "Origin of the tracktime server, e.g. https://api.tracktime.example. Empty means the deployed server, or the local dev server under `ray develop`.",
      "type": "textfield",
      "required": false,
      "placeholder": "https://tracktime.trebeljahr.com"
    },
    {
      "name": "webUrl",
      "title": "Web App URL",
      "description": "Origin of the tracktime web app, used for the device-pairing page and Open in Browser. Empty follows the API URL's default.",
      "type": "textfield",
      "required": false,
      "placeholder": "https://tracktime.trebeljahr.com"
    }
  ],
  "dependencies": {
    "@raycast/api": "^2.1.2",
    "@raycast/utils": "^2.3.0",
    "@starter/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.19.17",
    "@types/react": "19.0.10",
    "typescript": "^6.0.3"
  },
  "scripts": {
    "dev": "ray develop",
    "build": "ray build -e dist",
    "publish": "npx @raycast/api@latest publish",
    "typecheck": "tsc --noEmit"
  }
}
